# marketplace-promo-codes

Deploy repo for the **promo-codes** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs promo-codes --push`.

Coolify: base_directory `/`, dockerfile `/apps/promo-codes/Dockerfile`.
